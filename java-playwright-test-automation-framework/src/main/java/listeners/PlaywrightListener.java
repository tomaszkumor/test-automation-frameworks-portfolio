package listeners;

import org.testng.IInvokedMethodListener;
import org.testng.ITestListener;

public class PlaywrightListener implements ITestListener, IInvokedMethodListener {
    //TODO: ogarnij metode do robienia screenow przed i po kliknieciu + na failu.
    // Zrob to przed przygotowaniem pierwszego testu webowego.
}
